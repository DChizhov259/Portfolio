.navigation__menu {
  display: flex;
}

.navigation__item {
  margin-right: 60px;

  &:last-child {
    margin-right: 0;
  }
}

.navigation__link {
  font-family: "open-sans-regular";
  font-size: 16px;
  white-space: nowrap;
}
